export class ApplicationContext {
    constructor(geneticContext, canvasContext, dnaChartContext) {
        this.geneticContext = geneticContext
        this.canvasContext = canvasContext
        this.dnaChartContext = dnaChartContext
    }
}